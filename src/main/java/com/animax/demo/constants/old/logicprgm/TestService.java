package com.animax.demo.constants.old.logicprgm;

import java.util.Arrays;
import java.util.HashMap;
import java.util.List;

public class TestService {
    /*

write a braces/brackets/parentheses validator.

Let’s say:

‘(‘, ‘{‘, ‘[‘ are called “openers.”
‘)’, ‘}’, ‘]’ are called “closers.”
Write an efficient function that tells us whether or not an input string’s openers and closers are properly nested.

Examples:

“{ [] ( ) }” should return 0
“{ [( a ) ] }” should return 0
“{ [ }” should return 1
{[}] return 1
*/
    // order, numberof brackets {{ -2

    // key, value -> bracketType, number order by entry

//    public int checkBracketCorrectness(String input) {
//     //   HashMap<String>
//        List<String> splitStr = Arrays.asList(input.split(" "));
//        if(!splitStr.isEmpty()) {
//
//        }
//    }
}
