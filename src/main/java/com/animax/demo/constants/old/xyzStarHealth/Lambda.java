package com.animax.demo.constants.old.xyzStarHealth;

import java.util.concurrent.ExecutorService;
import java.util.concurrent.Executors;

public class Lambda {
    public static void main(String[] args) {

     //   System.out.println((5,6) -> CustomFunctionalInterface::add));



//        List<Integer> list = Arrays.asList(12,3,6,36,54);
//        int i = 5 + 6;
//        CustomFunctionalInterface integerPredicate = (5,6) -> 5+6;
//        List<Integer> newList = list.stream().filter(integerPredicate).collect(Collectors.toList());
//

        ExecutorService executorService = Executors.newFixedThreadPool(10);
    }
}
